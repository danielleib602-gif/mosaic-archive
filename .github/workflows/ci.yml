name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

env:
  RUSTUP_TOOLCHAIN: 1.96.0

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: ${{ matrix.os }} / Python ${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            python: "3.11"
          - os: ubuntu-latest
            python: "3.13"
          - os: ubuntu-latest
            python: "3.14"
          - os: windows-latest
            python: "3.11"
          - os: windows-latest
            python: "3.13"
          - os: macos-latest
            python: "3.13"
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python }}
      - uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          enable-cache: true
      - name: Install pinned Rust toolchain for the native Python package
        run: rustup toolchain install 1.96.0 --profile minimal
      - run: uv sync --frozen --extra dev
      - run: uv run --frozen python -m unittest discover -s tests -v
        env:
          MOSAIC_REQUIRE_NATIVE_EXTENSION: "1"

  quality-security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"
      - uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
      - name: Install pinned Rust toolchain for the native Python package
        run: rustup toolchain install 1.96.0 --profile minimal
      - run: uv sync --frozen --extra dev --extra release
      - run: uv run --frozen ruff check .
      - run: uv run --frozen mypy src
      - run: uv run --frozen --with coverage coverage run -m unittest discover -s tests
        env:
          MOSAIC_REQUIRE_NATIVE_EXTENSION: "1"
      - run: uv run --frozen --with coverage coverage report --fail-under=80
      - run: uv run --frozen --with bandit==1.9.4 bandit -q -r src -lll
      - run: uv run --frozen --with pip-audit pip-audit --local
      - run: uv run --frozen msc readiness --require-automatic --json
      - run: uv build
      - run: uv run --frozen python scripts/verify_python_artifact.py dist/*.whl dist/*.tar.gz
      - name: Build and smoke-test the source distribution in isolation
        shell: bash
        run: |
          smoke_python="$RUNNER_TEMP/mosaic-sdist-smoke/bin/python"
          uv venv --python 3.13 "$RUNNER_TEMP/mosaic-sdist-smoke"
          uv pip install --python "$smoke_python" dist/*.tar.gz
          "$smoke_python" scripts/smoke_native_preview.py
      - run: uv run --frozen python -m mosaic_archive.corpus .ci-corpus --unit-size 4096

  native-python-wheel:
    name: native Python wheel / ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
          - macos-15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
      - uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          enable-cache: true
      - name: Install pinned Rust toolchain
        run: rustup toolchain install 1.96.0 --profile minimal
      - name: Build the locked ABI3 native Python wheel
        run: >-
          uvx --from maturin==1.11.5 maturin build
          --manifest-path native/msc7-python/Cargo.toml
          --release --locked --out dist
      - name: Smoke-test the installed native Python wheel
        shell: pwsh
        run: |
          $env:PYTHONUTF8 = '1'
          $wheel = Get-ChildItem -LiteralPath dist -Filter '*.whl'
          if ($wheel.Count -ne 1) { throw "expected exactly one native wheel" }
          if ($wheel[0].Name -notmatch 'cp311-abi3') { throw "wheel is not CPython 3.11+ ABI3" }
          python scripts/verify_python_artifact.py $wheel[0].FullName
          python -m venv .wheel-smoke
          $smokePython = if ($IsWindows) { '.wheel-smoke/Scripts/python.exe' } else { '.wheel-smoke/bin/python' }
          & $smokePython -m pip install --disable-pip-version-check $wheel[0].FullName
          & $smokePython scripts/smoke_native_preview.py
          $env:MOSAIC_REQUIRE_NATIVE_EXTENSION = '1'
          & $smokePython -m unittest tests.test_native_extension -q

  rustsec-audit:
    name: RustSec dependency audit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Install pinned Rust toolchain
        run: rustup toolchain install 1.96.0 --profile minimal
      - name: Install pinned cargo-audit
        run: cargo +1.96.0 install cargo-audit --version 0.22.2 --locked
      - name: Audit locked Rust workspace dependencies
        run: cargo +1.96.0 audit --deny warnings --file Cargo.lock

  native-supervisor:
    name: native Linux supervisor
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"
      - uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
      - name: Install pinned Rust toolchain
        run: rustup toolchain install 1.96.0 --profile minimal --component rustfmt,clippy
      - run: uv sync --frozen
      - run: cargo +1.96.0 fmt --all --check
      - run: cargo +1.96.0 clippy --workspace --all-targets --locked -- -D warnings
      - run: cargo +1.96.0 test --workspace --locked
      - run: cargo +1.96.0 build --workspace --release --locked
      - name: Exercise a real delegated cgroup lifecycle
        shell: bash
        timeout-minutes: 5
        run: |
          set -euo pipefail
          cgroup_root=/sys/fs/cgroup
          parent="$cgroup_root/mosaic-native-ci-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          abi_leaf="$parent/mosaic-binding-abi-probe"
          negative_stderr=""
          [[ "$parent" =~ ^/sys/fs/cgroup/mosaic-native-ci-[0-9]+-[0-9]+$ ]] || {
            echo "refusing unsafe cgroup path: $parent" >&2
            exit 1
          }
          [[ "$abi_leaf" == "$parent/mosaic-binding-abi-probe" ]] || {
            echo "refusing unsafe ABI-probe cgroup path: $abi_leaf" >&2
            exit 1
          }

          cleanup() {
            local failed=0
            local session
            if [[ -n "$negative_stderr" ]]; then
              rm -f -- "$negative_stderr" || failed=1
            fi
            if [[ -d "$parent" ]]; then
              if [[ -d "$abi_leaf" ]]; then
                if [[ -e "$abi_leaf/cgroup.kill" ]]; then
                  printf '1' | sudo tee "$abi_leaf/cgroup.kill" >/dev/null || failed=1
                fi
                sudo rmdir -- "$abi_leaf" || failed=1
              fi
              if [[ -e "$parent/cgroup.kill" ]]; then
                printf '1' | sudo tee "$parent/cgroup.kill" >/dev/null || failed=1
              fi
              shopt -s nullglob
              for session in "$parent"/mosaic-supervisor-[0-9a-f]*; do
                [[ "${session##*/}" =~ ^mosaic-supervisor-[0-9a-f]{32}$ ]] || {
                  echo "refusing unexpected session child: $session" >&2
                  failed=1
                  continue
                }
                sudo rmdir -- "$session" || failed=1
              done
              sudo rmdir -- "$parent" || failed=1
            fi
            return "$failed"
          }
          trap 'cleanup || true' EXIT

          [[ "$(stat -fc %T "$cgroup_root")" == cgroup2fs ]]
          available=" $(<"$cgroup_root/cgroup.controllers") "
          for controller in cpuset memory pids; do
            [[ "$available" == *" $controller "* ]] || {
              echo "root cgroup lacks $controller" >&2
              exit 1
            }
            enabled=" $(<"$cgroup_root/cgroup.subtree_control") "
            if [[ "$enabled" != *" $controller "* ]]; then
              printf '+%s' "$controller" | sudo tee "$cgroup_root/cgroup.subtree_control" >/dev/null
            fi
          done

          sudo mkdir -- "$parent"
          uid="$(id -u)"
          gid="$(id -g)"
          sudo chown "$uid:$gid" -- \
            "$parent" \
            "$parent/cgroup.procs" \
            "$parent/cgroup.subtree_control" \
            "$parent/cgroup.threads"
          export MOSAIC_NATIVE_CGROUP_PARENT="$parent"

          set +e
          PYTHONPATH=src uv run --frozen python tools/run_native_cgroup_integration.py
          test_status=$?
          set -e
          if (( test_status == 0 )); then
            supervisor_binary="$PWD/target/release/mosaic-binding-supervisor"
            expected_probe_output='clone3 ABI probe passed (PID namespace and initial cgroup placement only); binding_eligible=false'
            sudo mkdir -- "$abi_leaf"
            probe_output="$(
              sudo bash -c 'exec 3<"$1"; exec "$2" --internal-clone3-abi-probe' \
                bash "$abi_leaf" "$supervisor_binary"
            )"
            printf '%s\n' "$probe_output"
            [[ "$probe_output" == "$expected_probe_output" ]] || {
              echo "unexpected clone3 ABI-probe success output" >&2
              exit 1
            }
            sudo rmdir -- "$abi_leaf"

            negative_stderr="$(mktemp -- "$RUNNER_TEMP/mosaic-clone3-negative.XXXXXX")"
            set +e
            negative_stdout="$(
              sudo bash -c 'exec 3<&-; exec "$1" --internal-clone3-abi-probe' \
                bash "$supervisor_binary" 2>"$negative_stderr"
            )"
            negative_status=$?
            set -e
            (( negative_status == 1 )) || {
              echo "clone3 ABI probe returned an unexpected closed-FD status: $negative_status" >&2
              exit 1
            }
            [[ -z "$negative_stdout" ]] || {
              echo "clone3 ABI-probe failure unexpectedly wrote to stdout" >&2
              exit 1
            }
            expected_negative_error='clone3 ABI probe failed; binding_eligible=false; cannot inspect clone3 ABI probe cgroup leaf FD 3: Bad file descriptor (os error 9)'
            negative_error="$(<"$negative_stderr")"
            [[ "$negative_error" == "$expected_negative_error" ]] || {
              echo "unexpected clone3 ABI-probe closed-FD diagnostic: $negative_error" >&2
              exit 1
            }
            rm -f -- "$negative_stderr"
            negative_stderr=""
          fi
          trap - EXIT
          cleanup_status=0
          cleanup || cleanup_status=$?
          if (( test_status != 0 )); then
            exit "$test_status"
          fi
          exit "$cleanup_status"

  native-supervisor-windows:
    name: native Windows supervisor
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
      - name: Install pinned Rust toolchain
        run: rustup toolchain install 1.96.0 --profile minimal --component rustfmt,clippy
      - run: cargo +1.96.0 fmt --all --check
      - run: cargo +1.96.0 clippy --workspace --all-targets --locked -- -D warnings
      - run: cargo +1.96.0 test --workspace --locked
      - run: cargo +1.96.0 build --workspace --release --locked

  native-core-macos:
    name: native macOS core
    runs-on: macos-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Install pinned Rust toolchain
        run: rustup toolchain install 1.96.0 --profile minimal --component rustfmt,clippy
      - run: cargo +1.96.0 fmt --all --check
      - run: cargo +1.96.0 clippy -p mosaic-msc7-core --all-targets --locked -- -D warnings
      - run: cargo +1.96.0 test -p mosaic-msc7-core --locked
      - run: cargo +1.96.0 build -p mosaic-msc7-core --release --locked
